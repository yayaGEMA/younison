{# Pour chaque message flash de type "success" existant, on l'affiche dans la structure HTML suivante #}
{% for message in app.flashes('success') %}

    <div class="row">
        <div class="alert alert-success text-center col-12 my-2">{{ message }}</div>
    </div>

{% endfor %}